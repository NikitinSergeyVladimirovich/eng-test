# Скопируйте в .env и при необходимости измените порты на хосте
# (если 3000 / 8080 / 5432 уже заняты).

API_HOST_PORT=3000
WEB_HOST_PORT=8080
POSTGRES_HOST_PORT=5432

# Опционально (см. docker-compose.yml)
# JWT_SECRET=
# ROUND_DURATION=60
# COOLDOWN_DURATION=30
